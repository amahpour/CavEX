# Build output directories (Wii devkitPro, native PC, ASan)
/build/
/build_pc/
/build_test/
/build_asan/
/build_ci/
/target/

# Compiled Wii binaries
*.dol
*.elf

# C build intermediates
*.o
*.d
*.map

# Autonomous test-rig screenshots (CAVEX_AUTOSHOT)
autoshot_*.png

# Capture-rig proof artifacts (issue #66) -- hosted as release assets, never
# committed (see scripts/capture_demo.sh).
*.gif
*.mp4

# Local dev handoff notes (not part of project history)
HANDOFF.md

# Python bytecode cache
__pycache__/
build_verify/
